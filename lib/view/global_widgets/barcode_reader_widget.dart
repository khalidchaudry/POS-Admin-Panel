import 'package:flutter/material.dart';
class BarcodeReaderWidget extends StatelessWidget {
  const BarcodeReaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}